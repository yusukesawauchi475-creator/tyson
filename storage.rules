rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Client SDK 直 Storage 操作は存在しない。画像/音声は API(Admin SDK) + signed URL 経由。
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
